#{
    connect: [
        rule "accept any connection" || accept(code(0, "welcome to the test")),
    ],

    mail: [
        rule "accept any sender" || accept(code(0, `We accept everybody, even '${mail_from().domain}'`)),
    ],

    rcpt: [
        rule "unhandled domain" || deny(code(550, `unhandled domain. sender='${mail_from().domain}', rcpt='${rcpt().domain}'`)),
    ],
}
