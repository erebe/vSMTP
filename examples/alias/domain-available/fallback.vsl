// Any unhandled domain in 'domain-available' will have it's recipient removed from the envelop.

#{
    "rcpt": [
        rule "prevent relay" || {
            remove_rcpt_envelop(rcpt());
            info(code554_7_1)
        },
    ]
}
